{
  "_comment": "TypeScript-error ratchet baseline. direction: down (can only shrink). `--update` only LOWERS it; when an upstream merge legitimately raises the count (their new code, not our regression), set `value` here by hand. 2965=v3.8.44 fork base; 3014=v3.8.45 (upstream added ~49 errors — verified our customizations add 0). Removal target: reach 0, then re-enable next.config typescript.ignoreBuildErrors:false.",
  "value": 3014,
  "direction": "down"
}
