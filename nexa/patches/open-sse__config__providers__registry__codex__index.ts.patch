diff --git a/open-sse/config/providers/registry/codex/index.ts b/open-sse/config/providers/registry/codex/index.ts
index d739e0982..e29c7b3d5 100644
--- a/open-sse/config/providers/registry/codex/index.ts
+++ b/open-sse/config/providers/registry/codex/index.ts
@@ -24,6 +24,65 @@ export const codexProvider: RegistryEntry = {
     tokenUrl: "https://auth.openai.com/oauth/token",
   },
   models: [
+    // ── GPT-5.6 series (Sol / Terra / Luna + -pro high-effort modes) ──
+    // GA 2026-07-09 (previewed 2026-06-26). These are the newest flagship Codex
+    // models on the ChatGPT/Codex OAuth backend for Pro subscribers. The slug is
+    // sent VERBATIM to chatgpt.com/backend-api/codex/responses — it IS the wire
+    // model id (executors/codex.ts splitCodexReasoningSuffix only strips
+    // -none/-low/-medium/-high/-xhigh effort suffixes, so "-pro" is preserved as a
+    // distinct backend slug, NOT a synthetic effort variant). This mirrors Hermes
+    // exactly (hermes_cli/codex_models.py DEFAULT_CODEX_MODELS + models.py
+    // openai-codex curated list), which surfaces the same eight slugs from the same
+    // OAuth subscription. Same OAuth backend context envelope as gpt-5.5: 400K
+    // window, ~272K usable input (#6191), 128K informational output cap.
+    {
+      id: "gpt-5.6-sol",
+      name: "GPT 5.6 Sol",
+      ...GPT_5_5_CODEX_CAPABILITIES,
+      contextLength: 400000,
+      maxInputTokens: 272000,
+      maxOutputTokens: 128000,
+    },
+    {
+      id: "gpt-5.6-sol-pro",
+      name: "GPT 5.6 Sol (Pro)",
+      ...GPT_5_5_CODEX_CAPABILITIES,
+      contextLength: 400000,
+      maxInputTokens: 272000,
+      maxOutputTokens: 128000,
+    },
+    {
+      id: "gpt-5.6-terra",
+      name: "GPT 5.6 Terra",
+      ...GPT_5_5_CODEX_CAPABILITIES,
+      contextLength: 400000,
+      maxInputTokens: 272000,
+      maxOutputTokens: 128000,
+    },
+    {
+      id: "gpt-5.6-terra-pro",
+      name: "GPT 5.6 Terra (Pro)",
+      ...GPT_5_5_CODEX_CAPABILITIES,
+      contextLength: 400000,
+      maxInputTokens: 272000,
+      maxOutputTokens: 128000,
+    },
+    {
+      id: "gpt-5.6-luna",
+      name: "GPT 5.6 Luna",
+      ...GPT_5_5_CODEX_CAPABILITIES,
+      contextLength: 400000,
+      maxInputTokens: 272000,
+      maxOutputTokens: 128000,
+    },
+    {
+      id: "gpt-5.6-luna-pro",
+      name: "GPT 5.6 Luna (Pro)",
+      ...GPT_5_5_CODEX_CAPABILITIES,
+      contextLength: 400000,
+      maxInputTokens: 272000,
+      maxOutputTokens: 128000,
+    },
     // gpt-5.5 codex OAuth backend caps context at 400K (not the public-API
     // 1.05M). Public refs : openai/codex#19208, #19319, #19464 ;
     // opencode#24171. max_output_tokens is stripped server-side
@@ -111,5 +170,38 @@ export const codexProvider: RegistryEntry = {
       supportsReasoning: true,
       supportsXHighEffort: true,
     },
+    // ── GPT-5.2 / GPT-5.1 Codex (older lineup, full-catalog parity) ──
+    // Added to match Hermes' complete codex_models.py lineup. Slug sent VERBATIM to
+    // chatgpt.com/backend-api/codex/responses (id IS the wire model id). Codex OAuth
+    // context window is 272K for all three per Hermes agent/model_metadata.py
+    // _CODEX_OAUTH_CONTEXT_FALLBACK (same envelope as gpt-5.5/gpt-5.6). NOTE: Hermes'
+    // codex_models.py documents that these three return HTTP 400 "not supported when
+    // using Codex with a ChatGPT account" on ChatGPT Pro accounts — availability is
+    // entitlement-gated by OpenAI, not something OmniRoute controls. Kept for lineup
+    // parity + forward-compat; auto-work if OpenAI enables them on the Codex backend.
+    {
+      id: "gpt-5.2-codex",
+      name: "GPT 5.2 Codex",
+      ...GPT_5_5_CODEX_CAPABILITIES,
+      contextLength: 400000,
+      maxInputTokens: 272000,
+      maxOutputTokens: 128000,
+    },
+    {
+      id: "gpt-5.1-codex-max",
+      name: "GPT 5.1 Codex Max",
+      ...GPT_5_5_CODEX_CAPABILITIES,
+      contextLength: 400000,
+      maxInputTokens: 272000,
+      maxOutputTokens: 128000,
+    },
+    {
+      id: "gpt-5.1-codex-mini",
+      name: "GPT 5.1 Codex Mini",
+      ...GPT_5_5_CODEX_CAPABILITIES,
+      contextLength: 400000,
+      maxInputTokens: 272000,
+      maxOutputTokens: 128000,
+    },
   ],
 };
