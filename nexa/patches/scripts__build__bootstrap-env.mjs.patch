diff --git a/scripts/build/bootstrap-env.mjs b/scripts/build/bootstrap-env.mjs
index 90ee46d..97f7487 100644
--- a/scripts/build/bootstrap-env.mjs
+++ b/scripts/build/bootstrap-env.mjs
@@ -19,7 +19,7 @@
  */
 
 import { randomBytes, createDecipheriv, scryptSync, createHash } from "node:crypto";
-import { existsSync, mkdirSync, readFileSync, writeFileSync } from "node:fs";
+import { chmodSync, existsSync, mkdirSync, readFileSync, writeFileSync } from "node:fs";
 import { createRequire } from "node:module";
 import { homedir } from "node:os";
 import { join, resolve } from "node:path";
@@ -136,7 +136,7 @@ function unquoteEnvValue(value) {
 }
 
 // ── Write a simple KEY=VALUE env file ───────────────────────────────────────
-function writeEnvFile(filePath, env) {
+export function writeEnvFile(filePath, env) {
   const lines = [
     "# Auto-generated by OmniRoute bootstrap — do not delete",
     `# Created: ${new Date().toISOString()}`,
@@ -144,7 +144,16 @@ function writeEnvFile(filePath, env) {
     ...Object.entries(env).map(([k, v]) => `${k}=${v}`),
     "",
   ];
-  writeFileSync(filePath, lines.join("\n"), "utf8");
+  // SECURITY_AUDIT M2: these files (server.env / .env) hold master secrets
+  // (STORAGE_ENCRYPTION_KEY, JWT_SECRET, API_KEY_SECRET). Create them 0600 so no
+  // other local user can read the at-rest encryption key. `mode` only applies on
+  // CREATE, so also chmod to tighten a pre-existing loose (0644) file.
+  writeFileSync(filePath, lines.join("\n"), { encoding: "utf8", mode: 0o600 });
+  try {
+    chmodSync(filePath, 0o600);
+  } catch {
+    // best-effort: non-POSIX FS (Windows) or perms — the create-mode already applied.
+  }
 }
 
 // ── Main bootstrap function ──────────────────────────────────────────────────
